//! Main plugin for replication.

use bevy::prelude::*;

/// Adds replication functionality.
pub struct ReplicationPlugin;

impl Plugin for ReplicationPlugin {
    fn build(&self, app: &mut App) {
        todo!();
    }
}